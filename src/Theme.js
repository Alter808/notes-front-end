import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#00BCD4',
      light: '#B2EBF2',
      dark: '#0097A7'
    },
    icons: {
      main: '#FFFFFF'
    },
    text: {
      primary: '#FFFFFF',
      secondary: '#757575'
    }
  }
})

export default theme
